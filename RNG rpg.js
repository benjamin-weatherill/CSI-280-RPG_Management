//Random Number Generator for RPG
function rng(min, max){
    let min = 0; //Should be set to zero
    let max = ""; //The higher the maximum the rarer the item, can be interchanged
    let itemDrop = ""; //item drop should be constant among all items if this equals the math.random number the item should drop
    if(itemDrop == Math.random(min, max)){
        return itemDrop;
    }
}
